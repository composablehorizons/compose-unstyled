{
  "name": "compose-unstyled-website",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:docs": "bun scripts/prepare-docs.js",
    "dev": "bun scripts/dev.js",
    "build": "DEMO_DISTRIBUTION=production bun scripts/refresh-demo-assets.js && bun run prepare:docs && astro build && bun scripts/check-build.js",
    "preview": "astro preview --host 127.0.0.1 --port 4321"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "yaml": "^2.9.1",
    "@astrojs/markdown-satteri": "^0.4.1"
  }
}
